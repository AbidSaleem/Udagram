apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend-service
spec:
  selector:
    app: udagram-frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8100
  type: ClusterIP