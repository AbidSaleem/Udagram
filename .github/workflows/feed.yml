name: CI - udagram-api-feed

on:
  push:
    paths:
      - "udagram-api-feed/**"
  pull_request:
    paths:
      - "udagram-api-feed/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        working-directory: udagram-api-feed
        run: npm install

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/udagram-api-feed:${{ github.run_number }} udagram-api-feed

      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/udagram-api-feed:${{ github.run_number }}
